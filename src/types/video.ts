export type VideoType = {
  flagged: boolean;
  owner: string;
  title: string;
  description: string;
  url: string;
  createDate: string;
  objectID: string;
  view: number;
  like: number;
  thumbnail: string;
  report: number;
};
