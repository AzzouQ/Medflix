import type { StylesType } from 'types';

const Styles: StylesType = {
  container: {
    display: 'flex',
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
    flexDirection: 'column',
  },
};

export { Styles };
