import type { StylesType } from 'types';

const Styles: StylesType = {
  grid: {
    height: '100%',
  },
  gridRow: {
    height: '100%',
    alignContent: 'center',
  },
  unsignedRow: {
    alignContent: 'center',
    justifyContent: 'center',
  },
};

export { Styles };
