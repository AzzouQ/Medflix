import type { StylesType } from 'types';

const Styles: StylesType = {
  container: {
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
  },
  icon: {
    fontSize: 20,
  },
};

export { Styles };
